{
  "name": "student-management-main",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "client": "npm run dev --prefix frontend",
    "server": "npm start --prefix backend",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"green,blue\" \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
